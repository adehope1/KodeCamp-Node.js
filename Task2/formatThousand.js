function formatThousand(num) {
    let digits =window.prompt("your digits here");
     digits = parseInt(digits);
    let Outputs = digits.toLocalString("");
    return num;
    
}
console.log(formatThousand(10000000));